#pragma once

// Copy to wifi_config.local.h and fill in only the local copy.
// Use a 2.4 GHz network with WPA2/WPA3-Personal.
namespace wifi_config {
inline constexpr char kSsid[] = "";
inline constexpr char kPassword[] = "";
}
